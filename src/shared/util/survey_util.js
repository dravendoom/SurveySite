// parses raw data from questions from the survey creator and converts it to JSON
function createSurveyJSON(){

}

// parses raw data from responses to survey from an individual user and converts it to JSON
function createResponseJSON(){
    
}